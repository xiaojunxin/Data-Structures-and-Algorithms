#include <iostream>
#include<string>
using std::istream;

/*void func(istream &is)
{
    std::string buf;
    while (is >> buf)
        std::cout << buf << std::endl;
    is.clear();
    //return is;
}
*/
int main()
{
	int a;
     //func(std::cin);
    //std::cout << is.rdstate() << std::endl;
	//istream &is;
	//is=std::cin£»
	//ostream &os=std::cout;
	std::cin>>a;
	std::cout<<a;
    return 0;
}